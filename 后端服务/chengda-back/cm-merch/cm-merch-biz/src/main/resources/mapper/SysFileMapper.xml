<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiaolong.cm.merch.mapper.SysFileMapper">


	<select id="selectFileListByIds" resultType="com.jiaolong.cm.merch.api.entity.SysFile">
		select id, file_name, bucket_name, original, brief, url, type, file_size, user_id, sort_order, if_default, create_time, update_time, del_flag
		from sys_file where id in
		<foreach collection="ids" index="i" item="id" open="(" close=")" separator=",">
			#{id}
		</foreach>
	</select>
</mapper>