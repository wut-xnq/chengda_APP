server:
  port: 4444

spring:
  application:
    name: upms
  cloud:
    nacos:
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: cloud
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: cloud
  config:
    import:
      - nacos:application.yml
      - nacos:${spring.application.name}.yml
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
cm:
  alipay:
    rate: 0.6 # 支付宝开放平台提现税率（当前官方设定日活量较低时为0.6%）
  wechatpay:
    rate: 0.6 # 微信开放平台提现税率（当前官方设定日活量较低时为0.6%）

file:
  bucketName: cbdfile
  local:
    enable: false
    base-path: /temp/chengda
  oss:
    enable: true
    endpoint: https://chengboda.com/minio
    access-key: cbdminioadmin
    secret-key: CBDminioQWE123