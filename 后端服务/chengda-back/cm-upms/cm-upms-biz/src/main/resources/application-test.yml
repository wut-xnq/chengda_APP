server:
  port: 4444

spring:
  application:
    name: upms
  cloud:
    nacos:
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: test
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        namespace: test
  config:
    import:
      - nacos:application.yml
      - nacos:${spring.application.name}.yml
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
cm:
  alipay:
    rate: 0.6
  wecharpay:
    rate: 0.6

file:
  bucketName: default-bucket
  oss:
    enable: true
    endpoint: http://localhost:9000
    access-key: Raclvk9h65JYisMAbd2R
    secret-key: BIuGiRmCfHwH97ai8omFdlzRJVHi16SdRfW9tdSI
